Fetch info from Itunes API about artists and their albums.

Rake task for fetching data is invoked like:
``` rake itunes:fetch[addicted2sounds] ```

Added primitive ember app to view fetched results. The application is based in client app.

